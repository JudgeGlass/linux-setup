---
- name: Hunter Wilcox - Linux Setup - SERVER
  hosts: localhost
  connection: local
  vars:
    home_dir: "{{ lookup('env', 'HOME') }}"
    username: "{{ lookup('env', 'USER') }}"
    git_username: "Hunter Wilcox"
    git_email: "hunter.wilcox@judgeglass.net"
    is_docker: "{{ lookup('env', 'IS_DOCKER') }}"
  roles:
    - common
    - server